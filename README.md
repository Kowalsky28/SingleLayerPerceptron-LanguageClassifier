# SingleLayerPerceptron-LanguageClassifier
A single-layer perceptron implementation for language detection using character frequency analysis. Written from scratch in Java (no machine learning libraries).
